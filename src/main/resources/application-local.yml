spring:
  jpa:
    hibernate:
      ddl-auto: 'create-drop'
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/laboratory
      username: root
      password: root
server:
  port: 8080